# Small experiment result archive

This directory preserves compact, machine-readable experiment results from the
rental server. It includes metrics JSON files, PTQ summaries and runtime
outlier statistics, analysis CSV/TSV files, and generated plots.

Datasets, caches, checkpoints, model weights, job logs, and PID files are not
included. `FILES.txt` records each archived source-relative path.
